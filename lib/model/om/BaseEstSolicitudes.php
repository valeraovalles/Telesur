<?php

/**
 * Base class that represents a row from the 'est_solicitudes' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Fri Sep 14 15:26:11 2012
 *
 * @package    lib.model.om
 */
abstract class BaseEstSolicitudes extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        EstSolicitudesPeer
	 */
	protected static $peer;

	/**
	 * The value for the id_solicitud field.
	 * @var        int
	 */
	protected $id_solicitud;

	/**
	 * The value for the id_solicitante field.
	 * @var        int
	 */
	protected $id_solicitante;

	/**
	 * The value for the hora_desde field.
	 * @var        string
	 */
	protected $hora_desde;

	/**
	 * The value for the hora_hasta field.
	 * @var        string
	 */
	protected $hora_hasta;

	/**
	 * The value for the estudio field.
	 * @var        string
	 */
	protected $estudio;

	/**
	 * The value for the fecha_solicitud field.
	 * @var        string
	 */
	protected $fecha_solicitud;

	/**
	 * The value for the id_producto field.
	 * @var        int
	 */
	protected $id_producto;

	/**
	 * The value for the observaciones field.
	 * @var        string
	 */
	protected $observaciones;

	/**
	 * The value for the estatus field.
	 * @var        string
	 */
	protected $estatus;

	/**
	 * The value for the dia_transmision field.
	 * @var        string
	 */
	protected $dia_transmision;

	/**
	 * @var        SfGuardUserProfile
	 */
	protected $aSfGuardUserProfile;

	/**
	 * @var        EstProductos
	 */
	protected $aEstProductos;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'EstSolicitudesPeer';

	/**
	 * Get the [id_solicitud] column value.
	 * 
	 * @return     int
	 */
	public function getIdSolicitud()
	{
		return $this->id_solicitud;
	}

	/**
	 * Get the [id_solicitante] column value.
	 * 
	 * @return     int
	 */
	public function getIdSolicitante()
	{
		return $this->id_solicitante;
	}

	/**
	 * Get the [optionally formatted] temporal [hora_desde] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getHoraDesde($format = 'H:i:s')
	{
		if ($this->hora_desde === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->hora_desde);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->hora_desde, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [hora_hasta] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getHoraHasta($format = 'H:i:s')
	{
		if ($this->hora_hasta === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->hora_hasta);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->hora_hasta, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [estudio] column value.
	 * 
	 * @return     string
	 */
	public function getEstudio()
	{
		return $this->estudio;
	}

	/**
	 * Get the [optionally formatted] temporal [fecha_solicitud] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaSolicitud($format = 'Y-m-d')
	{
		if ($this->fecha_solicitud === null) {
			return null;
		}



		try {
			$dt = new DateTime($this->fecha_solicitud);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_solicitud, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [id_producto] column value.
	 * 
	 * @return     int
	 */
	public function getIdProducto()
	{
		return $this->id_producto;
	}

	/**
	 * Get the [observaciones] column value.
	 * 
	 * @return     string
	 */
	public function getObservaciones()
	{
		return $this->observaciones;
	}

	/**
	 * Get the [estatus] column value.
	 * 
	 * @return     string
	 */
	public function getEstatus()
	{
		return $this->estatus;
	}

	/**
	 * Get the [dia_transmision] column value.
	 * 
	 * @return     string
	 */
	public function getDiaTransmision()
	{
		return $this->dia_transmision;
	}

	/**
	 * Set the value of [id_solicitud] column.
	 * 
	 * @param      int $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setIdSolicitud($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_solicitud !== $v) {
			$this->id_solicitud = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::ID_SOLICITUD;
		}

		return $this;
	} // setIdSolicitud()

	/**
	 * Set the value of [id_solicitante] column.
	 * 
	 * @param      int $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setIdSolicitante($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_solicitante !== $v) {
			$this->id_solicitante = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::ID_SOLICITANTE;
		}

		if ($this->aSfGuardUserProfile !== null && $this->aSfGuardUserProfile->getUserId() !== $v) {
			$this->aSfGuardUserProfile = null;
		}

		return $this;
	} // setIdSolicitante()

	/**
	 * Sets the value of [hora_desde] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setHoraDesde($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->hora_desde !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->hora_desde !== null && $tmpDt = new DateTime($this->hora_desde)) ? $tmpDt->format('H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->hora_desde = ($dt ? $dt->format('H:i:s') : null);
				$this->modifiedColumns[] = EstSolicitudesPeer::HORA_DESDE;
			}
		} // if either are not null

		return $this;
	} // setHoraDesde()

	/**
	 * Sets the value of [hora_hasta] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setHoraHasta($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->hora_hasta !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->hora_hasta !== null && $tmpDt = new DateTime($this->hora_hasta)) ? $tmpDt->format('H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->hora_hasta = ($dt ? $dt->format('H:i:s') : null);
				$this->modifiedColumns[] = EstSolicitudesPeer::HORA_HASTA;
			}
		} // if either are not null

		return $this;
	} // setHoraHasta()

	/**
	 * Set the value of [estudio] column.
	 * 
	 * @param      string $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setEstudio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->estudio !== $v) {
			$this->estudio = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::ESTUDIO;
		}

		return $this;
	} // setEstudio()

	/**
	 * Sets the value of [fecha_solicitud] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setFechaSolicitud($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fecha_solicitud !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fecha_solicitud !== null && $tmpDt = new DateTime($this->fecha_solicitud)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fecha_solicitud = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = EstSolicitudesPeer::FECHA_SOLICITUD;
			}
		} // if either are not null

		return $this;
	} // setFechaSolicitud()

	/**
	 * Set the value of [id_producto] column.
	 * 
	 * @param      int $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setIdProducto($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_producto !== $v) {
			$this->id_producto = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::ID_PRODUCTO;
		}

		if ($this->aEstProductos !== null && $this->aEstProductos->getIdProducto() !== $v) {
			$this->aEstProductos = null;
		}

		return $this;
	} // setIdProducto()

	/**
	 * Set the value of [observaciones] column.
	 * 
	 * @param      string $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setObservaciones($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->observaciones !== $v) {
			$this->observaciones = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::OBSERVACIONES;
		}

		return $this;
	} // setObservaciones()

	/**
	 * Set the value of [estatus] column.
	 * 
	 * @param      string $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setEstatus($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->estatus !== $v) {
			$this->estatus = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::ESTATUS;
		}

		return $this;
	} // setEstatus()

	/**
	 * Set the value of [dia_transmision] column.
	 * 
	 * @param      string $v new value
	 * @return     EstSolicitudes The current object (for fluent API support)
	 */
	public function setDiaTransmision($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->dia_transmision !== $v) {
			$this->dia_transmision = $v;
			$this->modifiedColumns[] = EstSolicitudesPeer::DIA_TRANSMISION;
		}

		return $this;
	} // setDiaTransmision()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id_solicitud = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->id_solicitante = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->hora_desde = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->hora_hasta = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->estudio = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->fecha_solicitud = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->id_producto = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->observaciones = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->estatus = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->dia_transmision = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = EstSolicitudesPeer::NUM_COLUMNS - EstSolicitudesPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating EstSolicitudes object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSfGuardUserProfile !== null && $this->id_solicitante !== $this->aSfGuardUserProfile->getUserId()) {
			$this->aSfGuardUserProfile = null;
		}
		if ($this->aEstProductos !== null && $this->id_producto !== $this->aEstProductos->getIdProducto()) {
			$this->aEstProductos = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(EstSolicitudesPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = EstSolicitudesPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSfGuardUserProfile = null;
			$this->aEstProductos = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(EstSolicitudesPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseEstSolicitudes:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				EstSolicitudesPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseEstSolicitudes:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(EstSolicitudesPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseEstSolicitudes:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseEstSolicitudes:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				EstSolicitudesPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSfGuardUserProfile !== null) {
				if ($this->aSfGuardUserProfile->isModified() || $this->aSfGuardUserProfile->isNew()) {
					$affectedRows += $this->aSfGuardUserProfile->save($con);
				}
				$this->setSfGuardUserProfile($this->aSfGuardUserProfile);
			}

			if ($this->aEstProductos !== null) {
				if ($this->aEstProductos->isModified() || $this->aEstProductos->isNew()) {
					$affectedRows += $this->aEstProductos->save($con);
				}
				$this->setEstProductos($this->aEstProductos);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = EstSolicitudesPeer::ID_SOLICITUD;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = EstSolicitudesPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setIdSolicitud($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += EstSolicitudesPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSfGuardUserProfile !== null) {
				if (!$this->aSfGuardUserProfile->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSfGuardUserProfile->getValidationFailures());
				}
			}

			if ($this->aEstProductos !== null) {
				if (!$this->aEstProductos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aEstProductos->getValidationFailures());
				}
			}


			if (($retval = EstSolicitudesPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = EstSolicitudesPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getIdSolicitud();
				break;
			case 1:
				return $this->getIdSolicitante();
				break;
			case 2:
				return $this->getHoraDesde();
				break;
			case 3:
				return $this->getHoraHasta();
				break;
			case 4:
				return $this->getEstudio();
				break;
			case 5:
				return $this->getFechaSolicitud();
				break;
			case 6:
				return $this->getIdProducto();
				break;
			case 7:
				return $this->getObservaciones();
				break;
			case 8:
				return $this->getEstatus();
				break;
			case 9:
				return $this->getDiaTransmision();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = EstSolicitudesPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getIdSolicitud(),
			$keys[1] => $this->getIdSolicitante(),
			$keys[2] => $this->getHoraDesde(),
			$keys[3] => $this->getHoraHasta(),
			$keys[4] => $this->getEstudio(),
			$keys[5] => $this->getFechaSolicitud(),
			$keys[6] => $this->getIdProducto(),
			$keys[7] => $this->getObservaciones(),
			$keys[8] => $this->getEstatus(),
			$keys[9] => $this->getDiaTransmision(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = EstSolicitudesPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setIdSolicitud($value);
				break;
			case 1:
				$this->setIdSolicitante($value);
				break;
			case 2:
				$this->setHoraDesde($value);
				break;
			case 3:
				$this->setHoraHasta($value);
				break;
			case 4:
				$this->setEstudio($value);
				break;
			case 5:
				$this->setFechaSolicitud($value);
				break;
			case 6:
				$this->setIdProducto($value);
				break;
			case 7:
				$this->setObservaciones($value);
				break;
			case 8:
				$this->setEstatus($value);
				break;
			case 9:
				$this->setDiaTransmision($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = EstSolicitudesPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setIdSolicitud($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setIdSolicitante($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setHoraDesde($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setHoraHasta($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setEstudio($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setFechaSolicitud($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setIdProducto($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setObservaciones($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setEstatus($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setDiaTransmision($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(EstSolicitudesPeer::DATABASE_NAME);

		if ($this->isColumnModified(EstSolicitudesPeer::ID_SOLICITUD)) $criteria->add(EstSolicitudesPeer::ID_SOLICITUD, $this->id_solicitud);
		if ($this->isColumnModified(EstSolicitudesPeer::ID_SOLICITANTE)) $criteria->add(EstSolicitudesPeer::ID_SOLICITANTE, $this->id_solicitante);
		if ($this->isColumnModified(EstSolicitudesPeer::HORA_DESDE)) $criteria->add(EstSolicitudesPeer::HORA_DESDE, $this->hora_desde);
		if ($this->isColumnModified(EstSolicitudesPeer::HORA_HASTA)) $criteria->add(EstSolicitudesPeer::HORA_HASTA, $this->hora_hasta);
		if ($this->isColumnModified(EstSolicitudesPeer::ESTUDIO)) $criteria->add(EstSolicitudesPeer::ESTUDIO, $this->estudio);
		if ($this->isColumnModified(EstSolicitudesPeer::FECHA_SOLICITUD)) $criteria->add(EstSolicitudesPeer::FECHA_SOLICITUD, $this->fecha_solicitud);
		if ($this->isColumnModified(EstSolicitudesPeer::ID_PRODUCTO)) $criteria->add(EstSolicitudesPeer::ID_PRODUCTO, $this->id_producto);
		if ($this->isColumnModified(EstSolicitudesPeer::OBSERVACIONES)) $criteria->add(EstSolicitudesPeer::OBSERVACIONES, $this->observaciones);
		if ($this->isColumnModified(EstSolicitudesPeer::ESTATUS)) $criteria->add(EstSolicitudesPeer::ESTATUS, $this->estatus);
		if ($this->isColumnModified(EstSolicitudesPeer::DIA_TRANSMISION)) $criteria->add(EstSolicitudesPeer::DIA_TRANSMISION, $this->dia_transmision);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(EstSolicitudesPeer::DATABASE_NAME);

		$criteria->add(EstSolicitudesPeer::ID_SOLICITUD, $this->id_solicitud);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getIdSolicitud();
	}

	/**
	 * Generic method to set the primary key (id_solicitud column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setIdSolicitud($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of EstSolicitudes (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setIdSolicitante($this->id_solicitante);

		$copyObj->setHoraDesde($this->hora_desde);

		$copyObj->setHoraHasta($this->hora_hasta);

		$copyObj->setEstudio($this->estudio);

		$copyObj->setFechaSolicitud($this->fecha_solicitud);

		$copyObj->setIdProducto($this->id_producto);

		$copyObj->setObservaciones($this->observaciones);

		$copyObj->setEstatus($this->estatus);

		$copyObj->setDiaTransmision($this->dia_transmision);


		$copyObj->setNew(true);

		$copyObj->setIdSolicitud(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     EstSolicitudes Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     EstSolicitudesPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new EstSolicitudesPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SfGuardUserProfile object.
	 *
	 * @param      SfGuardUserProfile $v
	 * @return     EstSolicitudes The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSfGuardUserProfile(SfGuardUserProfile $v = null)
	{
		if ($v === null) {
			$this->setIdSolicitante(NULL);
		} else {
			$this->setIdSolicitante($v->getUserId());
		}

		$this->aSfGuardUserProfile = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SfGuardUserProfile object, it will not be re-added.
		if ($v !== null) {
			$v->addEstSolicitudes($this);
		}

		return $this;
	}


	/**
	 * Get the associated SfGuardUserProfile object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SfGuardUserProfile The associated SfGuardUserProfile object.
	 * @throws     PropelException
	 */
	public function getSfGuardUserProfile(PropelPDO $con = null)
	{
		if ($this->aSfGuardUserProfile === null && ($this->id_solicitante !== null)) {
			$this->aSfGuardUserProfile = SfGuardUserProfilePeer::retrieveByPk($this->id_solicitante);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSfGuardUserProfile->addEstSolicitudess($this);
			 */
		}
		return $this->aSfGuardUserProfile;
	}

	/**
	 * Declares an association between this object and a EstProductos object.
	 *
	 * @param      EstProductos $v
	 * @return     EstSolicitudes The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setEstProductos(EstProductos $v = null)
	{
		if ($v === null) {
			$this->setIdProducto(NULL);
		} else {
			$this->setIdProducto($v->getIdProducto());
		}

		$this->aEstProductos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the EstProductos object, it will not be re-added.
		if ($v !== null) {
			$v->addEstSolicitudes($this);
		}

		return $this;
	}


	/**
	 * Get the associated EstProductos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     EstProductos The associated EstProductos object.
	 * @throws     PropelException
	 */
	public function getEstProductos(PropelPDO $con = null)
	{
		if ($this->aEstProductos === null && ($this->id_producto !== null)) {
			$this->aEstProductos = EstProductosPeer::retrieveByPk($this->id_producto);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aEstProductos->addEstSolicitudess($this);
			 */
		}
		return $this->aEstProductos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aSfGuardUserProfile = null;
			$this->aEstProductos = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseEstSolicitudes:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseEstSolicitudes::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseEstSolicitudes
